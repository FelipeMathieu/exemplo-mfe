{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "scripts": {
    "link": "lerna link",
    "start": "lerna run start --parallel",
    "build": "lerna run build",
    "serve": "lerna run serve --parallel"
  }
}
